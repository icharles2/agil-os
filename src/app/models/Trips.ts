export class Trip {
  cityImg?: string;
  title: string;
  destination: string;
  imgUrl?: string;
  origin: string;
  quality: number;
  transpo: string;
  lodging: string;
  departure: string;
  return: string;
  rental?: boolean;
  distance?: string;
}
