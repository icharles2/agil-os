export class Price {
  flight1?: number[]; // done
  flight2?: number[]; // done
  flight1Avg?: number;
  flight2Avg?: number;
  flightQ?: number; // done
  meals?: number[]; // done
  mealsQ?: number; // done
  gas?: number; // done
  gasTotal?: number; // done
  rental?: number[]; // done
  rentalQ?: number; // done
  hotel?: number[]; // done
  hotelQ?: number; // done
  flightTotal?: number;
  tripTotal?: number;
  mealsTotal?: number; // done
}
